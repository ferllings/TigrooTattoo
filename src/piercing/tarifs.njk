---
layout: layouts/base.njk
title: "Tarifs Piercing — Tigroo Tattoo"
permalink: /piercing/tarifs/
bodyclass: 'class="tarifs-piercing"'
extraCss: |
  <link rel="stylesheet" href="/css/tarifs.css">
---

<div class="tarifs-page">
  <h2>Tarifs Piercings</h2>

  <div class="tarifs-grid">
    {% for categorie in tarifsPiercing.categories %}
      <div class="tarifs-categorie">
        <h3>{{ categorie.nom }}</h3>
        <table>
          {% for p in categorie.piercings %}
            <tr>
              <td>{{ p.nom }}</td>
              <td>
                {% if p.prixDouble %}
                  <span>un {{ p.prix }}</span>
                  <span class="prix-double">double {{ p.prixDouble }}</span>
                {% else %}
                  {{ p.prix }}
                {% endif %}
              </td>
            </tr>
          {% endfor %}
        </table>
      </div>
    {% endfor %}
  </div>

  <p class="tarifs-footer">Avec ou sans RDV</p>

  <a href="/docs/Piercing Tarifs 2025.jpg" download class="btn-retour">Télécharger la grille tarifaire (JPG)</a>
  <a href="/" class="btn-retour">← Retour à l'accueil</a>
</div>
